"""
게임 설정 파일
모든 게임 상수와 설정값을 여기서 관리합니다.
"""

# =============================================================================
# 화면 설정
# =============================================================================
SCREEN_WIDTH = 80   # 콘솔 너비 (문자 단위)
SCREEN_HEIGHT = 50  # 콘솔 높이 (문자 단위)

# 맵 영역 (화면에서 맵이 표시되는 부분)
MAP_WIDTH = 80
MAP_HEIGHT = 43

# UI 영역
UI_HEIGHT = 7  # 하단 UI 높이

# =============================================================================
# 맵 생성 설정
# =============================================================================
ROOM_MAX_SIZE = 10
ROOM_MIN_SIZE = 6
MAX_ROOMS = 30

# =============================================================================
# 시야(FOV) 설정
# =============================================================================
FOV_ALGORITHM = 0  # 기본 FOV 알고리즘
FOV_LIGHT_WALLS = True
FOV_RADIUS = 10

# =============================================================================
# 생존 시스템 설정
# =============================================================================
# 최대 수치
MAX_HUNGER = 1000      # 최대 포만감
MAX_THIRST = 1000      # 최대 수분
MAX_BODY_TEMP = 100    # 체온 (정상: 36-38)
MAX_STAMINA = 100      # 최대 체력

# 감소율 (턴당)
HUNGER_DECAY = 1       # 턴당 배고픔 증가
THIRST_DECAY = 2       # 턴당 갈증 증가 (물이 더 급함)
STAMINA_RECOVERY = 1   # 턴당 체력 회복

# 위험 임계값
HUNGER_WARNING = 200   # 배고픔 경고
THIRST_WARNING = 200   # 갈증 경고
HUNGER_CRITICAL = 50   # 배고픔 위험
THIRST_CRITICAL = 50   # 갈증 위험

# =============================================================================
# 시간 시스템
# =============================================================================
TURNS_PER_HOUR = 60    # 1시간 = 60턴
HOURS_PER_DAY = 24     # 하루 = 24시간
DAWN_HOUR = 6          # 새벽
DUSK_HOUR = 20         # 황혼

# =============================================================================
# 색상 정의 (RGB)
# =============================================================================
class Colors:
    # 기본 색상
    WHITE = (255, 255, 255)
    BLACK = (0, 0, 0)
    RED = (255, 0, 0)
    GREEN = (0, 255, 0)
    BLUE = (0, 0, 255)
    YELLOW = (255, 255, 0)
    CYAN = (0, 255, 255)
    MAGENTA = (255, 0, 255)

    # 게임 전용 색상
    PLAYER = (255, 255, 255)        # 플레이어: 흰색
    WALL = (130, 110, 50)           # 벽: 갈색
    WALL_DARK = (0, 0, 100)         # 어두운 벽
    FLOOR = (200, 180, 50)          # 바닥: 밝은 갈색
    FLOOR_DARK = (50, 50, 150)      # 어두운 바닥

    # 엔티티 색상
    MONSTER = (255, 0, 0)           # 몬스터: 빨강
    ITEM = (128, 128, 255)          # 아이템: 파랑
    FOOD = (0, 255, 0)              # 음식: 초록
    WATER = (0, 128, 255)           # 물: 하늘색

    # UI 색상
    UI_BG = (20, 20, 30)            # UI 배경
    HEALTH_BAR = (0, 200, 0)        # 체력바
    HUNGER_BAR = (200, 150, 0)      # 배고픔바
    THIRST_BAR = (0, 150, 200)      # 갈증바

    # 상태 색상
    WARNING = (255, 255, 0)         # 경고: 노랑
    DANGER = (255, 0, 0)            # 위험: 빨강
    SAFE = (0, 255, 0)              # 안전: 초록

# =============================================================================
# ASCII 심볼 정의
# =============================================================================
class Symbols:
    # 지형
    WALL = '#'
    FLOOR = '.'
    DOOR_CLOSED = '+'
    DOOR_OPEN = '-'
    STAIRS_DOWN = '>'
    STAIRS_UP = '<'
    WATER = '~'
    TREE = 'T'
    GRASS = '"'
    FIRE = '*'

    # 엔티티
    PLAYER = '@'

    # 몬스터 (알파벳 = 종류)
    GOBLIN = 'g'
    ORC = 'o'
    TROLL = 'T'
    DRAGON = 'D'
    RAT = 'r'
    WOLF = 'w'
    BEAR = 'B'

    # 아이템
    FOOD = '%'
    POTION = '!'
    SCROLL = '?'
    WEAPON = ')'
    ARMOR = '['
    RING = '='
    WAND = '/'
    TOOL = '('
    CORPSE = '%'

    # 자원
    WOOD = '\\'
    STONE = '*'
    HERB = ';'
